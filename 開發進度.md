# Pixel Timer — 開發進度

## 產品定位

Windows 桌面像素風計時提醒器。桌面上放置像素桌寵，雙擊觸發倒數計時，時間到後桌寵播放完成動畫 + RPG 對話氣泡提醒。

---

## 技術棧

| 項目 | 技術 |
|------|------|
| 語言 | Python 3.12+ |
| GUI 框架 | PySide6（Qt for Python） |
| 素材生成 | Pillow（程式化產出 48×48 像素 PNG） |
| 置頂機制 | Win32 API `SetWindowPos` via ctypes |
| 設定儲存 | JSON 檔（`config/settings.json`） |
| 打包 | PyInstaller（Phase 4 預定） |

---

## Phase 進度

### Phase 1 — 核心引擎 ✅

> commit `1d7c104` / 2026-05-11

- `QSystemTrayIcon` 系統匣常駐
- `keyboard` 套件全域熱鍵觸發計時（後於 Phase 3 移除）
- `QTimer` 計時引擎，支援多組同時運行
- `QMessageBox` 簡易通知（後於 Phase 3 替換為 RPG 氣泡）
- `ConfigManager` JSON 設定讀寫

### Phase 2 — 設定 GUI ✅

> commit `18b5ee9` / 2026-05-11

- PySide6 三 Tab 設定視窗（熱鍵 / 一般 / 關於）
- 熱鍵錄製器 + 衝突偵測
- 設定即時寫入 JSON

### Phase 3 — 桌寵觸發系統 + 像素動畫 ✅

> commit `a9e4477` / 2026-05-11

**核心改造：** 移除 `keyboard` 套件與熱鍵系統，改為桌寵觸發。

新增模組：

| 檔案 | 行數 | 職責 |
|------|------|------|
| `ui/pet_widget.py` | 158 | 無框透明浮動視窗，拖曳定位、雙擊觸發計時、右鍵選單 |
| `ui/notification_window.py` | 109 | RPG 對話氣泡通知，QPainter 繪製像素邊框 + 三角指標 |
| `sprites/sprite_loader.py` | 41 | PNG 載入 + `(character, state, frame)` 快取 |
| `sprites/animation.py` | 78 | 動畫狀態機 idle/counting/finished，QTimer 驅動幀切換 |
| `sprites/generate_sprites.py` | 198 | Pillow 程式化產生像素 sprite |

改造模組：

| 檔案 | 改動 |
|------|------|
| `core/config_manager.py` | schema 從 hotkeys 改為 pets，新增 pet CRUD 方法 |
| `ui/settings_window.py` | 熱鍵 Tab 改為桌寵 Tab（角色/秒數/訊息 三欄表格） |
| `ui/tray_app.py` | 管理多隻桌寵生命週期 + 連接計時器信號 |

刪除：`core/hotkey_manager.py`、`requirements.txt` 中的 `keyboard`

### Phase 3.5 — 強制置頂 + 角色換血 ✅

> commit `e5b061a` / 2026-05-11

**強制置頂：**
- Win32 `SetWindowPos(HWND_TOPMOST)` via ctypes
- `_topmost_timer` 每 2 秒 re-assert
- `showEvent` 立即 assert
- `argtypes` 宣告 `ctypes.wintypes.HWND` 避免 64-bit 截斷

**角色換血（3 → 6）：**

| 角色 | 內部名 | 主色 |
|------|--------|------|
| 橘貓 | `orange_cat` | 橘 `#E65100` |
| 白貓 | `white_cat` | 灰白 `#78909C` |
| 三花 | `calico` | 棕 `#5D4037` |
| 史奴比 | `snoopy` | 黑白 `#212121` |
| 柴犬 | `shiba` | 棕 `#795548` |
| 哥布林 | `goblin` | 綠 `#1B5E20` |

- 36 張 sprite PNG（6 角色 × 3 state × 2 frame）
- 舊 config 自動遷移：`cat` → `orange_cat`、`dog` → `shiba`

---

## 架構設計

### 元件關係

```
main.py
  └─ QApplication
      ├─ ConfigManager（JSON 設定讀寫）
      ├─ TimerEngine（QTimer 多組計時）
      └─ TrayApp（QSystemTrayIcon）
           ├─ SpriteLoader（PNG 快取）
           ├─ PetWidget × N（浮動桌寵）
           │   └─ PetAnimator（狀態機驅動幀切換）
           ├─ NotificationWindow（RPG 氣泡）
           └─ SettingsWindow（三 Tab 設定）
```

### 設計原則

- 單一 PySide6 事件迴圈，不混用 threading / pygame
- 每隻桌寵 = 獨立 `QWidget`，自行管理拖曳與點擊
- 桌寵位置記錄在 config，重啟還原
- `NotificationWindow` 用 `dismissed` Signal + `deleteLater()`，不用 `WA_DeleteOnClose`
- 計時器狀態由 `timer_id` 管理，notification 關閉前若已重新計時則不強制 reset idle

### 動畫狀態機

| 狀態 | 幀間隔 | 視覺表現 |
|------|--------|----------|
| idle | 800ms | 微幅上下浮動 |
| counting | 400ms | 亮度提升 + 浮動加速 |
| finished | 300ms | 跳躍 + 星星特效 |

### Config Schema

```json
{
  "pets": [
    {
      "id": "pet_1",
      "character": "orange_cat",
      "duration_sec": 180,
      "message": "休息一下！",
      "position": {"x": 1200, "y": 750}
    }
  ],
  "global": {
    "sound_enabled": true
  }
}
```

---

## 檔案清單（1230 行）

| 檔案 | 行數 | 說明 |
|------|------|------|
| `main.py` | 22 | 入口：建 QApplication + 注入三大元件 |
| `core/config_manager.py` | 122 | JSON 設定 CRUD + legacy 遷移 |
| `core/timer_engine.py` | 61 | QTimer 多組計時，Signal 驅動 tick/finished |
| `ui/tray_app.py` | 229 | 系統匣 + 桌寵管理 + 通知生命週期 |
| `ui/pet_widget.py` | 158 | 桌寵浮動視窗（拖曳 / 雙擊 / 右鍵 / Win32 置頂） |
| `ui/settings_window.py` | 212 | 三 Tab 設定 GUI |
| `ui/notification_window.py` | 109 | RPG 對話氣泡通知 |
| `sprites/animation.py` | 78 | 動畫狀態機 |
| `sprites/sprite_loader.py` | 41 | PNG 載入 + 快取 |
| `sprites/generate_sprites.py` | 198 | Pillow 素材生成腳本 |

---

## 已知 Bug（待修）

### Bug 1：置頂不夠強

**現象：** 點工具列任一處、或滑鼠移至工具列作用中程式（出現預覽縮圖）時，桌寵被蓋住數秒後才回來。

**根因：** 2 秒 polling `SetWindowPos` 不夠即時，Windows 工具列有特殊 z-order 行為。

**可能方案：**
- `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)` 立即響應焦點切換
- 縮短 interval 到 500ms
- 加 `SWP_SHOWWINDOW` flag
- 研究 AppBar 層級的 z-order 規則

### Bug 2：淺色桌寵計時反饋不明顯

**現象：** 白貓等淺色角色 counting 狀態用 `brighter(body, 50)` 幾乎看不出差異。

**可能方案：**
- (A) 描邊閃爍 — counting 時交替加/移除 2px 亮色邊框
- (B) 搖擺動畫 — frame 間加 X 軸偏移模擬搖晃
- (C) overlay icon — 計時中在角落疊小沙漏/時鐘圖示
- (D) 色相偏移 — 不只加亮度，改變色相讓差異更大

---

## 下一步

- [ ] 修 Bug 1（置頂強化）
- [ ] 修 Bug 2（淺色反饋改善）
- [ ] Phase 4：PyInstaller 打包成 `.exe`

---

## 開發指令

```bash
# 啟動
.venv/Scripts/python main.py

# 安裝依賴
.venv/Scripts/pip install -r requirements.txt

# 重新生成 sprite 素材
.venv/Scripts/python sprites/generate_sprites.py
```

---

## 依賴

```
PySide6>=6.6.0
Pillow>=10.0.0
```
