# Pixel Timer — 開發進度

## 狀態

**專案狀態:** ✅ Phase 7 完成（2026-05-14）
**最新 commit:** `de5c544` — feat Phase 7: PyInstaller onedir 打包 + 預設值調整
**GitHub:** [dawang-sakura/pixel-timer](https://github.com/dawang-sakura/pixel-timer)（master = origin/master）
**下次行動:** 可選後續 — 自訂 .ico / UPX 壓縮 / Inno Setup 安裝程式
**Session archives:** Phase 1-7 全收，13 個 archive（`嘎筆記/session-archive/2026-05-1x-pixel-timer-*.md`）

---

## 產品定位

Windows 桌面像素風計時提醒器。桌面上放置像素桌寵，雙擊觸發倒數計時，時間到後桌寵播放完成動畫 + RPG 對話氣泡提醒。

---

## 技術棧

| 項目 | 技術 |
|------|------|
| 語言 | Python 3.12+ |
| GUI 框架 | PySide6（Qt for Python） |
| 素材生成 | Pillow（程式化產出 48×48 像素 PNG） |
| 置頂機制 | Win32 API `SetWinEventHook` + `SetWindowPos` via ctypes |
| 設定儲存 | JSON 檔（`config/settings.json`） |
| 打包 | PyInstaller 6.20（onedir 模式） |

---

## Phase 進度

### Phase 1 — 核心引擎 ✅

> commit `1d7c104` / 2026-05-11

- `QSystemTrayIcon` 系統匣常駐
- `keyboard` 套件全域熱鍵觸發計時（後於 Phase 3 移除）
- `QTimer` 計時引擎，支援多組同時運行
- `QMessageBox` 簡易通知（後於 Phase 3 替換為 RPG 氣泡）
- `ConfigManager` JSON 設定讀寫

### Phase 2 — 設定 GUI ✅

> commit `18b5ee9` / 2026-05-11

- PySide6 三 Tab 設定視窗（熱鍵 / 一般 / 關於）
- 熱鍵錄製器 + 衝突偵測
- 設定即時寫入 JSON

### Phase 3 — 桌寵觸發系統 + 像素動畫 ✅

> commit `a9e4477` / 2026-05-11

**核心改造：** 移除 `keyboard` 套件與熱鍵系統，改為桌寵觸發。

新增模組：

| 檔案 | 行數 | 職責 |
|------|------|------|
| `ui/pet_widget.py` | 158 | 無框透明浮動視窗，拖曳定位、雙擊觸發計時、右鍵選單 |
| `ui/notification_window.py` | 109 | RPG 對話氣泡通知，QPainter 繪製像素邊框 + 三角指標 |
| `sprites/sprite_loader.py` | 41 | PNG 載入 + `(character, state, frame)` 快取 |
| `sprites/animation.py` | 78 | 動畫狀態機 idle/counting/finished，QTimer 驅動幀切換 |
| `sprites/generate_sprites.py` | 198 | Pillow 程式化產生像素 sprite |

改造模組：

| 檔案 | 改動 |
|------|------|
| `core/config_manager.py` | schema 從 hotkeys 改為 pets，新增 pet CRUD 方法 |
| `ui/settings_window.py` | 熱鍵 Tab 改為桌寵 Tab（角色/秒數/訊息 三欄表格） |
| `ui/tray_app.py` | 管理多隻桌寵生命週期 + 連接計時器信號 |

刪除：`core/hotkey_manager.py`、`requirements.txt` 中的 `keyboard`

### Phase 3.5 — 強制置頂 + 角色換血 ✅

> commit `e5b061a` / 2026-05-11

**強制置頂：**
- Win32 `SetWindowPos(HWND_TOPMOST)` via ctypes
- `_topmost_timer` 每 2 秒 re-assert
- `showEvent` 立即 assert
- `argtypes` 宣告 `ctypes.wintypes.HWND` 避免 64-bit 截斷

**角色換血（3 → 6）：**

| 角色 | 內部名 | 主色 |
|------|--------|------|
| 橘貓 | `orange_cat` | 橘 `#E65100` |
| 白貓 | `white_cat` | 灰白 `#78909C` |
| 三花 | `calico` | 棕 `#5D4037` |
| 史奴比 | `snoopy` | 黑白 `#212121` |
| 柴犬 | `shiba` | 棕 `#795548` |
| 哥布林 | `goblin` | 綠 `#1B5E20` |

- 36 張 sprite PNG（6 角色 × 3 state × 2 frame）
- 舊 config 自動遷移：`cat` → `orange_cat`、`dog` → `shiba`

---

## 架構設計

### 元件關係

```
main.py
  └─ QApplication
      ├─ ConfigManager（JSON 設定讀寫）
      ├─ TimerEngine（QTimer 多組計時）
      └─ TrayApp（QSystemTrayIcon）
           ├─ SpriteLoader（PNG 快取）
           ├─ PetWidget × N（浮動桌寵）
           │   └─ PetAnimator（狀態機驅動幀切換）
           ├─ NotificationWindow（RPG 氣泡）
           └─ SettingsWindow（三 Tab 設定）
```

### 設計原則

- 單一 PySide6 事件迴圈，不混用 threading / pygame
- 每隻桌寵 = 獨立 `QWidget`，自行管理拖曳與點擊
- 桌寵位置記錄在 config，重啟還原
- `NotificationWindow` 用 `dismissed` Signal + `deleteLater()`，不用 `WA_DeleteOnClose`
- 計時器狀態由 `timer_id` 管理，notification 關閉前若已重新計時則不強制 reset idle

### 動畫狀態機

| 狀態 | 幀間隔 | 視覺表現 |
|------|--------|----------|
| idle | 800ms | 靜止無動作 |
| counting | 400ms | 上下浮動（無著色、無邊框） |
| finished | 300ms | 跳躍 + 星星特效 |

### Config Schema

```json
{
  "pets": [
    {
      "id": "pet_1",
      "character": "orange_cat",
      "duration_sec": 180,
      "message": "休息一下！",
      "position": {"x": 1200, "y": 750}
    }
  ],
  "global": {
    "sound_enabled": true
  }
}
```

---

## 檔案清單

| 檔案 | 說明 |
|------|------|
| `main.py` | 入口：建 QApplication + 注入 ConfigManager / TimerEngine / AlarmEngine |
| `core/constants.py` | 角色 ID 清單 + 中文顯示名對照 |
| `core/config_manager.py` | JSON 設定 CRUD + legacy 遷移 + alarm CRUD + atomic save |
| `core/timer_engine.py` | QTimer 多組計時，Signal 驅動 tick/finished |
| `core/alarm_engine.py` | QTimer 30s 掃描 HH:MM 鬧鐘觸發 + double-fire guard |
| `ui/tray_app.py` | 系統匣 + 桌寵管理 + timer/alarm signal 整合 |
| `ui/pet_widget.py` | 桌寵浮動視窗（拖曳 / 雙擊 / 右鍵 / 置頂 / 鬧鐘 tooltip） |
| `ui/settings_window.py` | 三 Tab 設定 GUI + 鬧鐘子區塊 |
| `ui/notification_window.py` | RPG 對話氣泡通知 |
| `sprites/animation.py` | 動畫狀態機 |
| `sprites/sprite_loader.py` | PNG 載入 + 快取 |
| `sprites/generate_sprites.py` | Pillow 素材生成（7 角色 + counting 描邊） |

---

## 已知 Bug — 已修復 ✅

- Bug 1 置頂：Phase 4 改用 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)` 即時響應
- Bug 2 淺色反饋：Phase 4 改用 counting frame 描邊閃爍

---

## Phase 4 — Bug 修復 + 角色增強 ✅

> commit TBD / 2026-05-11

**Bug 修復：**

- [x] Bug 1 置頂強化：`SetWinEventHook(EVENT_SYSTEM_FOREGROUND)` 即時響應焦點切換，取代 2 秒 polling
- [x] Bug 2 淺色反饋：counting frame 1 加 2px 黃色描邊閃爍（`draw_counting_border` 用 `getbbox` 定位）

**新增角色 — 小雞 `chick`：**

- [x] `generate_sprites.py` 新增 `draw_chick()` — 黃色系（主色 `#F9A825`），圓身 + 小翅 + 紅冠
- [x] 產出 6 張 sprite：`sprites/assets/chick/`（idle/counting/finished x 2 frame）
- [x] `notification_window.py` 加 `_CHARACTER_COLORS["chick"]`

**角色中文名：**

- [x] `core/constants.py` 新增 `CHARACTER_DISPLAY_NAMES` 對照表 + `CHARACTER_OPTIONS`
- [x] `settings_window.py` 桌寵 Tab 下拉選單顯示中文名（`_CHAR_ID_ROLE` 分離 pet_id 與 char_id）
- [x] `tray_app.py` tooltip 顯示中文角色名
- [x] 內部 ID 維持英文不動

**史奴比重繪：**

- [x] `draw_snoopy()` 改為側面抱愛心造型（對照參考圖）
- [x] `COLORS["snoopy"]` 藍色眼睛 `(70,130,180)` + 紅色愛心 `(205,60,70)`

**審查修復：**

- [x] hook 安裝移至 `__init__` 末尾（防 HWND 未就緒）
- [x] callback 迭代 `list(cls._all_widgets)` 防突變
- [x] `setEditorData` fallback 改為明確 `"orange_cat"` 預設

角色清單（7 隻）：

| 中文名 | 內部 ID | 主色 |
|--------|---------|------|
| 橘貓 | `orange_cat` | `#E65100` |
| 白貓 | `white_cat` | `#78909C` |
| 三花 | `calico` | `#5D4037` |
| 史奴比 | `snoopy` | `#212121` |
| 柴犬 | `shiba` | `#795548` |
| 哥布林 | `goblin` | `#1B5E20` |
| 小雞 | `chick` | `#F9A825` |

---

## Phase 4.1 — Snoopy 重繪 + 動畫行為調整 ✅

> commit `7108d9d` / 2026-05-11

**Snoopy 重繪（outline-first 方法）：**

- [x] 黑色輪廓 → 白色內部（2px inset） → 內部細節（耳溝、黑眼、鼻子、斜嘴）
- [x] 改採 outline-first 繪製方式，符合 LEGO 參考圖形式
- [x] 視覺效果：黑白輪廓分明，細節清晰

**動畫行為統一（所有角色）：**

- [x] idle：靜止，兩幀都無位移（`dy=0`）
- [x] counting：上下浮動（`dy=0,1`），無著色、無邊框
- [x] finished：保持不變（跳躍 + 星星）

**死代碼清理：**

- [x] 移除 `tint` 參數（7 個 `draw_*()` 函式）
- [x] 移除 `"eye"` 鍵自 `COLORS["snoopy"]`（眼睛改用 `dark` 色繪製）
- [x] 移除 `draw_counting_border()`、`COUNTING_BORDER`、`brighter`、`counting_tint` 相關代碼

**檔案變動：**

| 檔案 | 變動 |
|------|------|
| `generate_sprites.py` | 287 → 256 行（`draw_snoopy()` 重寫 + 死碼移除） |

---

## Phase 4.2 — Snoopy BRIK 像素圖案直抄 ✅

> commit `58564c2` / 2026-05-12

**改動：**

- [x] 廢棄手繪 `draw_snoopy()`，改用 BRIK.co 像素圖案精確抽取
- [x] 抽取方法：LEGO 積木參考圖 → BOX resample 29x28 grid → flood fill 標記白色身體
- [x] pixel map 29x28 格 → NEAREST scale 42x40 → 貼上 48x48 畫布
- [x] 尺寸對齊：42x40 與小雞（32x40）同級，視覺重量匹配其他寵物
- [x] 顏色：純黑描邊、純白身體、藍色眼睛（55,110,185）、紅色愛心（225,35,35）

**檔案變動：**

| 檔案 | 變動 |
|------|------|
| `generate_sprites.py` | `_SNOOPY_MAP` 28 行 pixel map + `draw_snoopy()` resize 方式重寫 |
| `sprites/assets/snoopy/*.png` | 6 張 sprite 全部重生 |

---

## Phase 5 — Cron 鬧鐘功能 ✅

> commit TBD / 2026-05-13

**新增模組：**

| 檔案 | 行數 | 職責 |
|------|------|------|
| `core/alarm_engine.py` | 53 | QTimer 30 秒掃描 HH:MM 觸發鬧鐘，double-fire guard，smart cache clear |

**改動模組：**

| 檔案 | 改動 |
|------|------|
| `core/config_manager.py` | pet schema 加 `alarms` 欄位 + migration + CRUD（`get_pet_alarms`/`set_pet_alarms`/`disable_alarm`）+ atomic save（`os.replace`） |
| `main.py` | 新增 `AlarmEngine` 初始化 + 注入 `TrayApp` |
| `ui/tray_app.py` | 接收 `alarm_engine`，連接 `alarm_triggered` signal → `finished` 動畫 + `NotificationWindow`，`once` 自動 disable + widget alarm refresh |
| `ui/pet_widget.py` | 存 `_alarms` + `_character_name`，`_compute_next_alarm()` 計算下一個鬧鐘時間，idle tooltip 顯示角色名 + 下個鬧鐘 |
| `ui/settings_window.py` | 鬧鐘子區塊（`QTableWidget` 4 欄：時間/訊息/重複/啟用）+ `RepeatDelegate` + `_alarms_by_row` 管理 + 時間格式驗證與正規化 |

**Config Schema：**

```json
{
  "id": "pet_1",
  "character": "orange_cat",
  "duration_sec": 180,
  "message": "休息一下！",
  "alarms": [
    {"time": "09:00", "message": "上班打卡！", "repeat": "daily", "enabled": true},
    {"time": "12:30", "message": "午餐時間", "repeat": "weekdays", "enabled": true}
  ],
  "position": {"x": 1200, "y": 750}
}
```

**repeat 選項：** `once`（觸發後自動 disable）/ `daily` / `weekdays`（週一至五）

**審查修復：**

- [x] `_on_add_alarm` / `_on_delete_alarm` 補 `_sync_alarm_table_to_data()` 防資料遺失
- [x] `once` disable 加 `enabled` re-check 防 stale index 誤 disable
- [x] `clear_fired_cache()` 保留當前分鐘 entries 防 settings save 導致 double-fire
- [x] `config_manager.save()` 改 atomic write（`os.replace`）
- [x] 時間格式正規化：`"9:00"` → `"09:00"`（`strftime` 匹配）
- [x] `PetWidget.refresh_alarms()` — `once` disable 後即時更新 tooltip

---

## Phase 6 — GUI 像素風古早遊戲介面 ✅

> commit TBD / 2026-05-13

**字體：** Ark Pixel Font 12px（OFL-1.1，繁中 CJK 全支援），proportional + monospaced 兩款。`QFontDatabase.addApplicationFont` 載入，`setPixelSize(12)` 確保 DPI 不變形。

**暗色主題（RPG Dark Blue）：**

| 角色 | 色碼 |
|------|------|
| 背景底色 | `#1A1A2E` |
| 背景中色 | `#16213E` |
| 邊框亮面 | `#E2E2E2` |
| 邊框暗面 | `#8888AA` |
| 文字 | `#FFF1E8`（PICO-8 暖白） |
| 游標 | `#FFD700`（金色） |

QPalette 全域設定 + QSS 統一按鈕/表格/下拉/輸入框/捲軸/選單/tooltip 像素邊框。

**新增模組：**

| 檔案 | 職責 |
|------|------|
| `ui/pixel_theme.py` | 色彩常數 + 字體載入 + QPalette + QSS + `apply_theme()` |
| `assets/fonts/*.ttf` | Ark Pixel 12px proportional + monospaced（zh_tw） |

**改造模組：**

| 檔案 | 改動 |
|------|------|
| `ui/settings_window.py` | 全面重寫：`PixelTabBar`（RPG `▶` 游標 Tab）+ `SpritePreview`（即時角色動畫預覽）+ `TimeDelegate`（QSpinBox 時間選擇器）+ 雙邊框 `paintEvent` + `_displayed_alarm_row` 修復鬧鐘 sync bug |
| `ui/notification_window.py` | 暗底 RPG 雙邊框 + 角色主題色內框線 + 打字機逐字顯示（`QTimer` 40ms）+ 點擊跳過 + 5s 自動關閉改可停止 `QTimer` |
| `main.py` | 啟動時呼叫 `apply_theme(app)` |
| `ui/tray_app.py` | 傳遞 `sprite_loader` 給 `SettingsWindow` |
| `ui/dwm_utils.py` | `DwmSetWindowAttribute` 加 `try/except` 支援 Win10 |

**施工清單：**

- [x] 像素字體引入（Ark Pixel 12px TTF，`QFontDatabase` 載入）
- [x] QPalette + QSS 全域暗色主題（按鈕/輸入框/下拉/表格/捲軸統一像素邊框）
- [x] 設定視窗背景改為深色 + RPG 雙邊框（外亮內暗）
- [x] Tab 切換改為 RPG 選單式橫排選項（`▶` 金色游標 + 鍵盤左右切換）
- [x] 桌寵預覽：選中桌寵時即時顯示 64x64 sprite 動畫
- [x] 鬧鐘時間欄改 `TimeDelegate`（QSpinBox 0-23/0-59 + wrapping）
- [x] 通知氣泡升級：暗底 RPG 邊框 + 角色主題色內框線 + 打字機逐字顯示 + 點擊跳過

**審查修復：**

- [x] `NotificationWindow` 自動關閉改可停止 `QTimer`（防 use-after-free）
- [x] `SpritePreview` 加 `closeEvent` 防禦性 timer 清理
- [x] `_sync_alarm_table_to_data` 改用 `_displayed_alarm_row` 追蹤（修復換選 pet 時鬧鐘資料覆寫 bug）
- [x] `_on_delete_alarm` 改先刪再 sync（防 stale data 存檔）
- [x] `RepeatDelegate` 改用 `_REPEAT_ID_ROLE` 避免角色碰撞
- [x] `PixelTabBar` 快取字體（避免 `paintEvent` 每幀重建 QFont）
- [x] `_load_fonts()` 加冪等 guard

---

## Phase 6.4 — UI 精修

用戶回報 8 項，分批處理：A 先做，視覺實機檢視後追加 V1-V5 round 解掉 B2/B3/B4 + C1。

**批次 A — 氣泡+預覽重設計 ✅**

> commit `f8d84a8` → fix `fb9e2ac`（11 項 review issue）/ 2026-05-13

- [x] A1：抽出 `ui/bubble_widget.py`（QPainter 層疊框 + tail + 4-corner bite + shadow + 打字機 index）；NotificationWindow 改用 BubbleWidget
- [x] A2：設定預覽區改用 BubbleWidget 即時顯示桌寵綁定 message；移除「鬧鐘設定 — 角色名」靜態標籤；`itemChanged` 連線 col 0 角色色變、col 2 message 即時更新

**Visual Round 1 — 實機檢視後追加 ✅**

> commit `4188071` → fix `e56ac06`（4 important + 5 nits）/ 2026-05-14

實機看到 A 視覺沒到位 + B2/B3/B4 + C1 一併解掉：

- [x] V1：bubble 外緣 2px→1px，色由 `TEXT` 改 `accent.darker(180)` — 去除「咖色底」厚重感
- [x] V2：tail seam 修法（`_draw_tail_bottom` 在 tail 開口處 cream-bridge body 底邊三層）+ 4 corner studs（rivet on cream，2×2 `accent_dark`）
- [x] V3（= B2 + B3）：鬧鐘「啟用」改 `setCellWidget(QWidget + QHBoxLayout + stretches + QCheckBox)` 置中 + 暖色主題；`pixel_theme.py` QSS 加 `QTableWidget::item:focus { outline: 0 }` 去虛線框
- [x] V4（= B4）：時間 column 100→160px，TimeDelegate SpinBox 52→60px，margins/spacing 加 1
- [x] V5（= C1）：新 `ui/title_bar.py` `PixelTitleBar`（drag + pixel X button hover/press states + optional icon_pixmap），SettingsWindow `FramelessWindowHint` + `setFixedSize(560, 520)` + `showEvent` 接 `disable_dwm_frame`，X → `self.reject`

**Visual Round 2 — B1 + B5 卡片視圖重設計 ✅**

> branch `phase-6.4-b1-b5-card-redesign`（10 commits，等實機 review 後 merge master）/ 2026-05-14

本輪把兩張 QTableWidget（桌寵 + 鬧鐘）全面重做成 RPG menu 風 row card 視圖，三隻 delegate 隨之廢除；所有 inline editor 文字統一靠中對齊。

**新建模組：**

| 檔案 | 行數 | 職責 |
|------|------|------|
| `ui/card_list_view.py` | 179 | QScrollArea + QVBoxLayout 通用卡片容器，selection / add / remove / index_of / iter_cards |
| `ui/pet_card.py` | 203 | PetCard：sprite 縮圖 + 角色下拉 + 秒數 spin + 訊息 edit + 刪除 X，paintEvent 雙層邊框 + selected 金色游標條 |
| `ui/alarm_card.py` | 181 | AlarmCard：hour/min spin + 訊息 edit + 重複下拉 + 啟用 checkbox + 刪除 X |

**改造模組：**

| 檔案 | 改動 |
|------|------|
| `ui/settings_window.py` | 793 → 506 行：拆掉 `pet_table` / `_alarm_table` + 3 個 delegate，改用 CardListView 承載；`_build_pet_tab` 拆成 `_build_pet_tab_ui` + `_load_pet_data`；保留 `settings_changed` Signal、`__init__` 簽名、`_alarms_by_row` 資料結構 |
| `ui/pixel_theme.py` | +38 行 QSS：DeleteButton hover/press、卡片內 input 統一 border；移除過時 QTableWidget QSS |
| `core/constants.py` | 加 `DEFAULT_CHARACTER = "orange_cat"`，settings_window 4 處 magic string 改用 |

**廢碼清理（G7）：** 砍掉 `TimeDelegate` / `RepeatDelegate` / `CharacterDelegate` 三個類別 + `PET_COLUMNS` / `ALARM_COLUMNS` / `_CHAR_ID_ROLE` / `_REPEAT_ID_ROLE` 常數，import 中 `QTableWidget` / `QTableWidgetItem` / `QHeaderView` / `QStyledItemDelegate` 移除。

**B1 編輯置中（5 個欄位）：**

- [x] PetCard 角色 QComboBox（`setEditable(True)` + `lineEdit().setAlignment(AlignCenter)` + `setReadOnly(True)`）
- [x] PetCard 秒數 QSpinBox `setAlignment(AlignCenter)`
- [x] PetCard 訊息 QLineEdit AlignCenter
- [x] AlarmCard 時間 hour/min QSpinBox AlignCenter
- [x] AlarmCard 訊息 QLineEdit AlignCenter

**審查修復（8 important + 6 nit）：**

- [x] code-I1：刪除選中 pet 時鬧鐘 data resurrection — `_displayed_alarm_row = None` + `alarm_list.clear()` 移到 `remove_card` 之前
- [x] code-I2：QComboBox 非 editable 無法置中 — 改 editable + ReadOnly + AlignCenter
- [x] code-I3：`_rewire_all` blanket disconnect — 卡片身上存 `_listview_slot` ref，targeted disconnect
- [x] py-I1：CardListView `_cards` 私有外洩 — 加 `index_of` / `iter_cards` public API
- [x] py-I2：`_build_pet_tab` 53 行混三件事 — 拆成 ui builder + data loader
- [x] py-I3：`_alarms_by_row` reindex 改 dict comprehension
- [x] py-I4：PetCard / AlarmCard paintEvent 加 `try/finally: painter.end()`，N5/N6/N8 同步補
- [x] py-I5：alarm_card.py 死匯入 `BORDER_HI` 移除

**架構決策：**

- 卡片繼承 `QWidget`（不繼承 `QFrame`）配合 `WA_StyledBackground=False`，避免 QSS background 蓋掉 paintEvent
- selection 由容器（CardListView）管理，卡片只負責 `set_selected(bool)` 觸發 `update()`，不持有 selection state
- 資料流策略：lazy sync — 卡片 always editable，但只在切 pet / 新增/刪除 alarm / save 時統一抓資料（避免訊號風暴）；bubble preview / sprite preview 即時更新 UI 但不寫 data store
- alarm card 不畫 selected 態，只畫 hover（簡化）

**檔案變動統計：**

| 統計 | 數字 |
|------|------|
| 新增檔案 | 3（pet_card / alarm_card / card_list_view） |
| 改動檔案 | 3（settings_window / pixel_theme / constants） |
| 淨變動 | +738 / -425 = +313 行 |
| Commit | 10 |

**批次 B 剩餘（待下次施工）**
→ 本輪 Visual Round 2 完成，見上方章節

---

## Phase 7 — PyInstaller 打包 ✅

> commit `de5c544` / 2026-05-14

- [x] `core/paths.py` 集中路徑解析（`BUNDLE_DIR` / `APP_DIR` / `SPRITES_DIR` / `FONTS_DIR` / `CONFIG_PATH`）
- [x] `config_manager.py`、`sprite_loader.py`、`pixel_theme.py` 改用 `paths.py`，移除各自的 `Path(__file__)` 硬編碼
- [x] PyInstaller onedir spec（`pixel_timer.spec`）：打包 `sprites/assets/` + `assets/fonts/`，排除 tkinter/unittest
- [x] `build.bat` 一鍵打包腳本（自動安裝 pyinstaller==6.20.0）
- [x] 首次執行自動建 `config/settings.json`（原有機制，config 寫在 exe 旁而非 `_internal/` 內）
- [x] 打包後實測：exe 啟動 OK、sprite 載入 OK、config 生成正確
- [x] 預設值調整：1 隻小G、180 秒、"時間到" + 每日 17:00 鬧鐘

**產出：** `dist/PixelTimer/`（128 MB，含 PySide6 runtime）

---

## Bug 修復 — Win11 DWM 灰色邊框 ✅

> commit `f5e8e33` / 2026-05-13

**問題：** Windows 11 build 26200 的 DWM 對所有 `FramelessWindowHint + Tool` 視窗加上深色邊框 + 圓角，導致桌寵和通知氣泡出現灰色底框。`WA_TranslucentBackground` 只處理內容區透明，不影響 DWM 邊框。

**已嘗試無效方案：** `WA_NoSystemBackground`、`setStyleSheet`、空 `paintEvent`、`setMask(createAlphaMask)`、`DwmExtendFrameIntoClientArea`（造成白底）、`BypassWindowManagerHint`

**解法：** `DwmSetWindowAttribute` 設定 4 個屬性：

| 屬性 | 值 | 效果 |
|------|-----|------|
| `DWMWA_NCRENDERING_POLICY` (2) | `DWMNCRP_DISABLED` (2) | 關閉 NC 渲染 |
| `DWMWA_WINDOW_CORNER_PREFERENCE` (33) | `DWMWCP_DONOTROUND` (1) | 關閉圓角 |
| `DWMWA_BORDER_COLOR` (34) | `COLOR_NONE` (0xFFFFFFFE) | 移除邊框色 |
| `DWMWA_SYSTEMBACKDROP_TYPE` (38) | `DWMSBT_NONE` (1) | 關閉系統背景 |

抽為 `ui/dwm_utils.py` 共用模組，`PetWidget` 和 `NotificationWindow` 在 `showEvent` 呼叫。

---

## 下一步

Phase 7 完成。可選後續：
- 自訂 `.ico` 圖示
- UPX 壓縮減小體積
- 安裝程式（Inno Setup / NSIS）

---

## 開發指令

```bash
# 啟動
~/.venvs/pixel_timer/Scripts/python main.py

# 安裝依賴
~/.venvs/pixel_timer/Scripts/pip install -r requirements.txt

# 重新生成 sprite 素材
~/.venvs/pixel_timer/Scripts/python sprites/generate_sprites.py
```

---

## 依賴

```
PySide6>=6.6.0
Pillow>=10.0.0
```
